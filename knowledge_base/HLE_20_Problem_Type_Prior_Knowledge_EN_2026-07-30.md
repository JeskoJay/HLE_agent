Keywords: two-step substitution cipher, substitution cipher, monoalphabetic substitution, known-plaintext attack, cryptanalysis, frequency analysis, ciphertext pattern. Relevant knowledge: A monoalphabetic substitution preserves letter-repetition patterns and approximate word lengths, while a second substitution may replace one ciphertext symbol with several characters and introduce artificial spaces; known plaintext can reveal a partial ciphertext-to-plaintext mapping. Solving approach: Recover a partial monoalphabetic mapping from high-confidence auxiliary words, compare repeated patterns to locate characters disrupted by the second layer, reverse that layer before decrypting the monoalphabetic cipher, and verify the candidate by re-encryption. Common pitfalls: Treating artificial spaces as word boundaries, accepting a fluent sentence without re-encryption, or applying frequency analysis before reversing the second layer.
Keywords: Shamir Secret Sharing, SageMath, finite field, GF(29), random polynomial, secret shares, leading coefficient, missing-value attack. Relevant knowledge: Shamir sharing depends not only on its threshold but also on the coefficient distribution; some random-polynomial APIs generate a polynomial of exactly the requested degree, forcing the leading coefficient to be nonzero and changing the support of observable values. Solving approach: Write equations for the low-degree polynomial and leaked shares, use finite-field linear combinations to eliminate selected random coefficients, determine the support of the remaining coefficient, and inspect missing values across repeated samples. Common pitfalls: Confusing exactly degree d with degree at most d, forgetting modular arithmetic, treating a theoretically impossible value as a mere frequency anomaly, or ignoring the random-generator API semantics.
Keywords: Bacon cipher, binary encoding, five-bit code, snake traversal, path decoding, character grid, binary alphabet. Relevant knowledge: Two symbols naturally represent binary digits, five-bit groups often indicate a Bacon-style alphabet, and a two-dimensional layout may require border, spiral, or snake traversal. Solving approach: Find the path endpoints and turning rule, try both reading directions, map the symbols to bits, group them at plausible fixed widths, test common alphabet-index conventions, and write the result back along the path for verification. Common pitfalls: Assuming row-major reading, ignoring the reverse direction, fixing the symbol-to-bit mapping too early, or stopping after obtaining only a partial word.
Keywords: SageMath, exact arithmetic, Python preparser, symbolic ring, XOR operator, rational arithmetic, sqrt simplification, Python type semantics. Relevant knowledge: Sage normally preparses .sage files, whereas an ordinary .py file retains Python operator semantics; in Python, ^ is bitwise XOR rather than exponentiation, integer true division may introduce floats, symbolic radicals may require explicit simplification, and symbolic equality results need a type check. Solving approach: Trace every operator, operand type, parent ring, exactness property, simplification form, and final output type, then reproduce the behavior with a minimal example under the target SageMath version. Common pitfalls: Applying interactive Sage syntax directly to a .py file, checking only approximate numerical equality, assuming every square root simplifies automatically, or assuming symbolic comparison always returns a Python Boolean.
Keywords: biometric authentication, challenge-response, replay attack, liveness detection, authentication intent, template protection, presentation attack, deepfake detection. Relevant knowledge: Biometrics are generally not secrets, and authentication designs must separately evaluate template leakage, replay resistance, liveness, active intent, modality independence, sensor trust, and presentation attacks. Solving approach: Convert the prompt into a checklist of security properties, assess every candidate mechanism against each property, distinguish fresh challenges from static matching, encryption, risk scoring, and additional factors, and state both mitigated attacks and residual dependencies. Common pitfalls: Treating any security improvement as satisfying every requirement, equating template secrecy with liveness, equating liveness with user intent, or ignoring sensor compromise and deepfake risk.
Keywords: DNS AitM, Adversary-in-the-Middle, MITRE T1557, multi-vantage verification, trust distribution, DNSSEC, quorum validation, firmware security. Relevant knowledge: When the operating system, firmware, network, or a single data source may be compromised, single-point hardening is insufficient; multi-vantage validation distributes trust across independent devices, networks, resolvers, protocols, and reverse queries. Solving approach: Map trust boundaries and attacker capabilities first, determine whether each proposal still depends on an untrusted component, and for multi-source designs analyze failure-domain independence, quorum rules, disagreement handling, and residual risk at the presentation layer. Common pitfalls: Declaring one hardware or software component absolutely trusted, confusing confidentiality with integrity, treating user authentication as DNS-data authentication, or describing mitigation as complete prevention.
Keywords: robot arm, serial manipulator, inverse kinematics, collision avoidance, link thickness, joint angle constraint, torque constraint, vector geometry. Relevant knowledge: A cylindrical link radius follows from its circumference, the minimum bend angle of thick links follows from surface clearance rather than centerline intersection, a serial arm endpoint is the sum of directed link vectors, and gravitational torque depends on link weights and center-of-mass lever arms. Solving approach: Convert thickness and clearance into angular inequalities, represent cumulative orientations with vectors or complex numbers, optimize shoulder-to-end-effector distance over the feasible region, and check nonadjacent-link collisions, all joint torque limits, and numerical boundaries. Common pitfalls: Optimizing only the endpoint, checking only the shoulder joint, confusing circumference with diameter, mixing angle units, or accepting an unverified local optimum.
Keywords: distortion-free watermarking, language model watermark, exponential race, Gumbel-max trick, entropy lower bound, token distribution, pseudorandom function, expectation bound. Relevant knowledge: The negative logarithm of a uniform random variable is exponentially distributed, probability-scaled sampling rules can be transformed into exponential races, the winning index preserves the target categorical distribution, and the expected detection statistic can often be bounded through a nonnegative series and entropy. Solving approach: Apply a negative-log transform, rewrite the power maximization as a minimum of exponential variables, use exponential-race probabilities and independence, compute the conditional one-token expectation, expand it as a series, and convert a logarithmic bound into an entropy term through probability weighting. Common pitfalls: Incorrectly making the winning index dependent on the minimum time, omitting probability weighting, mixing logarithm bases, or interchanging expectation and summation without convergence justification.
Keywords: sentence embeddings, heuristic matching, logistic regression, linear separability, Boolean function, XOR, equivalence, feature engineering. Relevant knowledge: Logistic regression represents linearly separable labels in its input feature space; same-dimension product and absolute-difference features can linearize some Boolean relations, whereas two raw cross-dimension binary variables should be analyzed at the four corners of the Boolean plane. Solving approach: Write a truth table for each operation, plot positive and negative cases in the x-y plane, inspect whether the engineered features include a product, absolute difference, or constant, and either construct a separating line or prove convex-hull intersection. Common pitfalls: Transferring nonseparability from the raw space to the expanded feature space, forgetting the intercept, mixing same-dimension with cross-dimension features, or applying the classic XOR result to every Boolean relation.
Keywords: ReLU network, matrix rank, latent representation, activation pattern, multilayer perceptron, rank bound, batch matrix, nonlinear transformation. Relevant knowledge: Linear transformations obey input-rank and dimension bounds, but elementwise ReLU is nonlinear and may reduce or increase the rank of a batch representation matrix; matrix dimensions still impose an absolute upper bound. Solving approach: Record each batch-matrix shape and linear rank bound, analyze activation patterns separately, use positive and negative support splitting to construct rank-increasing cases, and apply the final representation width to eliminate impossible claims. Common pitfalls: Assuming rank(ReLU(X)) never exceeds rank(X), ignoring biases and sample-specific activation patterns, considering input rank without output width, or confusing an existence claim with a statement about all weights.
Keywords: overlap-add, overlap-save, FFT convolution, DFT, IDFT, block processing, FIR filter, linear convolution. Relevant knowledge: With FFT length N and filter length M, each block contributes N-M+1 new valid samples; overlap-add partitions the input and adds output segments, while overlap-save discards the contaminated prefix of each circular-convolution block, and full linear convolution also requires the output tail. Solving approach: Determine whether the prompt counts individual transforms, DFT-IDFT pairs, or the one-time filter-spectrum computation, then calculate input length, full output length, valid step size, and ceiling block counts under one consistent convention. Common pitfalls: Omitting the convolution tail, treating the FFT length as the valid block length, confusing block count with transform count, or failing to state whether the filter DFT is included.
Keywords: Bagua architecture, ternary computing, custom data types, Schwarzschild metric, gravitational time dilation, Kepler's law, binomial approximation, memory optimization. Relevant knowledge: Kepler's circular-orbit relation can eliminate the central mass, the stationary Schwarzschild clock factor can be expressed in dimensionless form, and a custom ternary architecture may require fractional expressions and low-order approximations because of restricted types, literals, and functions. Solving approach: Complete the symbolic physics derivation and dimensional check in SI units, identify the small parameter, select an approximation order that meets the requested precision, express constants in supported integer or fraction form, compare approximation error with the rounding margin, and count variable storage under the stated rules. Common pitfalls: Confusing altitude above the surface with radial distance from the center, using unsupported square roots or decimal literals, checking only one sample without an error bound, or mixing temporary-expression storage with the defined variable count.
Keywords: Wuxing architecture, decimal computing, projectile motion, interception problem, Newton's method, square-root approximation, custom fraction type, memory counting. Relevant knowledge: Without air resistance, horizontal projectile displacement follows from flight time and horizontal velocity, a moving target has position linear in time, eliminating time often yields a quadratic equation in launch speed, and Newton iteration can replace a missing square-root primitive. Solving approach: Eliminate time symbolically, verify whether mass cancels, check angle constants, gravity units, and physical root selection, implement with supported types and literals, compare numerical error with output precision, and count memory exactly as specified. Common pitfalls: Mixing degrees and radians, selecting a nonphysical root, rounding constants before error analysis, or incorrectly including or excluding parameters, locals, and temporaries.
Keywords: C code golf, scanf, variadic function, format string, undefined behavior, endianness, ABI, memory optimization, shortest program. Relevant knowledge: The scanf format argument must point to a NUL-terminated character sequence, conversion specifications must match variadic pointer types, interpreting integer bytes as a string depends on endianness and object layout, and incorrect types, out-of-bounds writes, or signed overflow can cause undefined behavior; correctness, variable-memory minimization, and source-length minimization are separate goals. Solving approach: Judge defined behavior under ISO C first, build a separate model for any specified compiler and ABI, then analyze type shrinking, variable reuse, and source transformations after fixing the code-golf counting rules. Common pitfalls: Treating one successful machine execution as standard-C correctness, replacing undefined-behavior analysis with compiler warnings, claiming minimality without fixed rules, or conflating memory and character optimization.
Keywords: maximum state entropy, intrinsic motivation, state visitation distribution, surprise reward, negative log probability, fixed-point analysis, exploration policy, entropy optimization. Relevant knowledge: State entropy is the expected negative log probability under the state distribution; with the old distribution fixed, negative log visitation probability defines a linear objective over state distributions reachable by the new policy, and its coefficients differ from the entropy gradient only by a normalization-irrelevant constant. Solving approach: Assuming a limit exists, write the fixed-point optimality condition, compare it with the first-order condition for concave entropy maximization, and distinguish the roles of the initial policy, finite iterations, and the limiting policy. Common pitfalls: Omitting the convergence assumption, treating an arbitrary finite iterate as maximum entropy, ignoring unreachable states and the feasible distribution set, or applying formal arithmetic to policies without preserving normalization.
Keywords: finite-memory POMDP, deterministic finite automaton, DFA distinguishability, binary observation sequence, memory state, corridor POMDP, policy memory, exhaustive enumeration. Relevant knowledge: When the action is selected only after an observation history ends, a finite-memory policy can be modeled as a deterministic finite automaton reading the observation string; two histories can trigger different actions only if the automaton finishes in different memory states. Solving approach: Rewrite the problem as distinguishability of equal-length binary strings by DFAs of different sizes, use classifier signatures or enumeration at short lengths, search for a witness pair at a candidate length, enumerate transition functions for the lower bound, construct a larger automaton for the upper bound, and connect distinguishability to terminal rewards. Common pitfalls: Giving a distinguishing automaton without proving smaller ones impossible, omitting the initial state or state renamings, confusing transition functions with accepting states, or failing to translate distinguishability into expected return.
Keywords: Schwarzschild time dilation, pulsar, gravitational redshift, binomial expansion, weak-field approximation, Taylor series, custom architecture, memory optimization. Relevant knowledge: The stationary-clock factor outside a spherical nonrotating object follows from the Schwarzschild metric; if distance is measured above the surface, the radial coordinate is the object radius plus that height, and a small dimensionless parameter permits binomial or Taylor approximation of the square root. Solving approach: Compute the Schwarzschild radius and expansion parameter, decide whether the weak-field approximation meets the precision target, use the next omitted term to estimate error, rewrite the expression using supported integer and fractional forms, and count variable memory under the custom architecture. Common pitfalls: Treating surface altitude as center radius, mixing meters and kilometers, truncating without checking the rounding boundary, or ignoring special rules for macros, parameters, and temporaries.
Keywords: Python truthiness, Boolean operations, short-circuit evaluation, and operator, or operator, set, tuple, zip, iterable. Relevant knowledge: Empty containers are false and nonempty containers are true, and and or return an operand rather than coercing the result to Boolean; tuple truthiness depends on the outer container, and sets are unordered but remain valid iterables. Solving approach: Apply short-circuit rules step by step, record the concrete returned object, type, and truth value, test set identities with overlapping sets, test indexed expressions with nonempty tuples containing false values, and execute a minimal example under the target Python version. Common pitfalls: Assuming and/or return only True or False, treating unordered sets as non-iterable, comparing only truth values while ignoring returned operands, or overlooking that short-circuiting prevents later expressions from running.
Keywords: Python division semantics, Python 2, Python 3, true division, floor division, floating-point precision, integer conversion, negative operands, IEEE 754. Relevant knowledge: Python 3 integer true division returns a float, Python 2 uses legacy integer division for two integer operands, floor division rounds toward negative infinity, integer conversion from a float normally truncates toward zero, and explicit conversion beyond the exact binary-float integer range can change the computational path. Solving approach: Annotate the Python version, operand types, evaluation order, and result type for each expression; test universal claims with negative and nondivisible integers, floating-point precision boundaries, overflow, and underflow; distinguish numerical equality from type identity; and consult the official language reference. Common pitfalls: Treating floor division as truncation toward zero, assuming floating-point expressions may be freely reassociated, testing only small positive integers, or ignoring version and future-import semantics.
Keywords: Domain Model, Martin Fowler, Anemic Domain Model, Controller, Domain Service, Service Layer, rich domain model, responsibility assignment, domain-driven design. Relevant knowledge: A Domain Model combines data with behavior, a Controller handles request entry and flow, domain entities protect invariants that naturally belong to them, domain services hold cross-object behavior that fits no single entity or value object, and application services coordinate transactions and external resources. Solving approach: Assign each business rule to the object with the information required to enforce it, separate domain rules from workflow coordination and infrastructure effects, inspect the design for controller bloat, god objects, or an anemic domain model, and evaluate whether entity-service boundaries remain explicit. Common pitfalls: Placing all logic wherever invocation is convenient, confusing domain services with application services, reducing entities to getters and setters, or embedding external notification effects directly in core domain objects.
