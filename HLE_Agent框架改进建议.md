# HLE Agent 框架改进建议

> 结合 [datawhalechina/hello-agents《从零开始构建智能体》](https://github.com/datawhalechina/hello-agents) 的 Agent 构建方法论，对当前 HLE 解题智能体框架做诊断与改进规划。
> 文档结构：现状定位 → 分模块改进（每块含"当前问题 / 理论依据 / 改进方案 / 落地位置 / 优先级"）→ 演进路线图。

---

## 0. 现状定位（先说清楚"我们站在哪"）

当前框架是一条**多智能体流水线（Pipeline）**：

```
Sentinel(域分类) → RAG(向量检索) → Solver(3 分支并发: systematic/intuitive/code_first)
                → Arbiter(仲裁聚合) → Exporter(落盘)
```

对照 hello-agents 第一章的分类，它属于 **"多智能体协作 → 组织化工作流（MetaGPT/CrewAI 式 SOP）"**，本质更接近 **Workflow（按部就班执行）** 而非 **Agent（赋予自由度自主达成目标）**（见 1.4.3 Workflow 与 Agent 差异）。

**已经做对的（不要推翻）**：
- ✅ 多分支并行求解 + 仲裁聚合（多智能体协作范式），比单 agent 更稳；
- ✅ RAG 用 bge 向量检索 + Markdown 标题分块（与 8.3.4 智能分块策略一致）；
- ✅ 每题独立流式推理日志、并发控制（Semaphore）、RAG 可开关（`--no-rag`）；
- ✅ 评测链路打通（准确率 45% vs 无 RAG 20%，证明 RAG 有效）。

**实测基线**（20 题子集）：
| 版本 | Accuracy | Calibration Error |
|------|----------|-------------------|
| 含 RAG | 45.0% ± 21.8% | 51.43 |
| 无 RAG | 20.0% ± 17.5% | 55.33 |

CE 高达 ~51 说明**模型过度自信、校准严重不足**——这是后续改进的核心抓手之一。

---

## 1. 规划模块：补齐显式 Plan（对应第四章 Plan-and-Solve）

**当前问题**
Solver 三个分支直接"拿到题就答"，没有先把复杂问题拆成子步骤。HLE 多为多步推理/多子问（如 overlap-add 计数、多部分证明），缺乏规划易"偏离轨道"（这正是 Plan-and-Solve 要解决的核心痛点，4.3）。

**理论依据**
> 第四章："Plan-and-Solve 将任务处理明确分为规划(Plan)与执行(Solve)两阶段……解决思维链在多步复杂问题易偏离轨道的问题。适用于结构性强、可清晰分解的任务——多步数学题、整合多源报告、代码生成。"（4.3 / 表 4.1）

**改进方案**
- 在 `solver.solve_branch` 前新增 **Planner**：对单题先产出分步计划 `P=[s1,s2,...]`，再逐步执行，前步结果喂后步；
- 计划写入 `_meta`，并进入每题日志，提升可解释性；
- 仅对 `answer_type` 为 exactMatch / 多步计算题启用，选择题可跳过以省 token。

**落地位置**：`hle_agent/solver.py` 新增 `planner.py`，`pipeline.py` 调用。
**优先级**：🔴 高（直接关联多步题错因）。

---

## 2. 反思闭环：最大缺口，补上 Reflection（对应第四章 Reflection）

**当前问题**
Arbiter 只做"聚合三个分支答案 → 选一个"，**没有事后自我批判与修正**。初稿一旦生成即结束，无法发现逻辑漏洞或事实错误。这是当前框架与成熟 Agent 范式最大的差距。

**理论依据**
> 第四章："Reflection 为智能体引入事后自我校正循环：执行→反思→优化。适用于对最终结果质量/准确性要求极高、实时性宽松的场景——关键代码、技术报告、科研推演、决策支持。"（4.4 / 表 4.1）

**改进方案**
- 在 Arbiter 之前/之中加入 **Critic + Refine** 双步：
  1. Critic 独立评审三分支答案，指出事实错误 / 逻辑漏洞 / 数值偏差（这正是我们观察到的"多选漏判元素 B/J""数值末位偏差"）；
  2. Refiner 结合反馈产出 `final_answer_v2`；
  3. 设 `max_reflect=1~2` 安全阀，避免无限迭代。
- 这能直接拉低 CE（当前 51 的过度自信会被反思纠正）、提升硬题准确率。

**落地位置**：`hle_agent/arbiter.py` 升级为 `arbiter_reflect.py`（含 critic/refine 双调用）。
**优先级**：🔴 最高（性价比最高的单一改动）。

---

## 3. 工具循环结构化：把工具调用改造成 ReAct Loop（对应第四章 ReAct）

**当前问题**
`code_first` 分支会调用代码工具，但目前是"一次性生成代码→跑"，没有显式的 **Thought→Action→Observation** 循环，遇到报错只能靠分支级容错，缺乏动态纠错（日志里已观察到工具沙箱 traceback 被吞）。

**理论依据**
> 第四章："ReAct 形成'思考-行动-观察'循环，具备动态规划与纠错能力、高可解释性。适用于需外部工具/精确计算/API 交互的任务。"（4.2）"执行效率问题：多次串行调用，耗时与费用高"——需加 `max_steps` 安全阀（4.2.4）。

**改进方案**
- 把 `tools.py` 代码执行包装为 ReAct 循环：`Thought` 决定下一步 → `Action` 调 `run_code` → `Observation` 回收 stdout/报错 → 再思考，直到 `max_steps`（如 5）或 self-done；
- Observation（含报错）回灌上下文，让模型自我修复代码；
- 加 `max_steps` 安全阀控制成本。

**落地位置**：`hle_agent/tools.py` + `solver.py` 的 code_first 分支。
**优先级**：🟠 中（提升计算题鲁棒性）。

---

## 4. 记忆系统：跨题经验无法复用（对应第八章 记忆系统）

**当前问题**
框架**完全没有记忆层**：每题独立推理，上一题踩过的坑、某类题型的正确解法，下一题完全用不上。HLE 多题同属"信号处理/替换密码"等主题，重复踩坑。

**理论依据**
> 第八章把智能体记忆分四层：工作记忆（会话内、TTL）、情景记忆（交互事件回顾）、语义记忆（抽象知识/规则）、感知记忆（多模态）。并强调无记忆导致"对话遗忘、个性化缺失、学习能力受限"（8.1.2）。

**改进方案（分阶段，参考 Extra10 自进化路线）**
- **L1 工作记忆**：单题内保留 Planner 步骤与工具 Observation（已在 §1/§3 覆盖）；
- **L2 语义/情景记忆（跨题）**：增加一个轻量 `memory_store`（文件或向量库），存"题型→已验证解法"。例如 overlap-add 的计数公式、替换密码破译套路，命中同域新题时注入 prompt；
- **L3 失败经验库**：把被判错的题的 Critic 反馈沉淀，未来同类题主动规避。

**落地位置**：新增 `hle_agent/memory.py`，`pipeline.py` 在 RAG 后追加 memory 检索。
**优先级**：🟠 中（长期收益大，先建 L2 最小可用版）。

---

## 5. RAG 检索增强：从"能用"到"精准"（对应第八章 8.3.5）

**当前问题**
当前 RAG = bge 向量检索 top-k 直出，缺**重排**与**查询扩展**。对于措辞与知识库差异大的题，召回精度有提升空间（这恰是 RAG 版比无 RAG 多对的那 6 题的来源，需进一步放大）。

**理论依据**
> 第八章高级检索策略（8.3.5）：
> - **MQE 多查询扩展**：用 LLM 生成多样表述并行检索合并去重，"擅长处理用词多样性问题"；
> - **HyDE 假设文档嵌入**：先让 LLM 写假设答案段，用其向量检索，"擅长处理语义鸿沟问题"；
> - 建议："一般查询启用 MQE；专业领域同时启用 MQE+HyDE"。

**改进方案**
- 加 **rerank 层**：检索候选池扩到 `k×candidate_multiplier`（如 4 倍），再用 cross-encoder 或小模型 rerank 取 top-k；
- 加 **MQE**：每题生成 3 个改写 query 合并召回；专业/数学题再叠加 HyDE；
- 加 **混合检索**：向量 + BM25 关键词互补（知识库是结构化 Markdown，关键词命中很准）。

**落地位置**：`hle_agent/rag_retriever.py` 增加 `rerank`、`mqe`、`hybrid` 三件套。
**优先级**：🟢 低-中（边际收益，但实现成本低）。

---

## 6. 仲裁与置信度校准（对应第十二章 + 当前 CE=51 痛点）

**当前问题**
Arbiter 输出的 `confidence` 与真实正确率严重不匹配（CE≈51，即模型常报 100 却答错）。评测报告中"两版都错 10 题"里大量是**多选漏判/数值偏差**，说明仲裁缺少对答案完整性的交叉验证。

**理论依据**
> 第十二章强调评估要分维度、分层级；校准误差本质是"置信度≠准确率"。GAIA 的"难度递进下降率"思路可借鉴：按题型/难度分层看准确率。

**改进方案**
- **Self-Consistency 置信度**：每分支不止采样 1 次，而是采样 `k`（如 3~5）次取多数票，`confidence = 一致比例`。这比"模型自报 100"可靠得多，能直接压低 CE；
- **仲裁打分 rubric**：Arbiter 按"正确性/完整性/一致性"三维打分，多选题强制校验集合元素是否齐全（针对漏判 B/J 的错因）；
- **分层报告**：评测按 `answer_type`（exactMatch vs multipleChoice）与 `domain` 拆开看准确率，定位弱项。

**落地位置**：`arbiter.py`（多维打分）、`solver.py`（k 次采样）、`run_judge_results.py`（分层指标）。
**优先级**：🔴 高（校准是 HLE 级难题的生命线）。

---

## 7. 自洽性与投票：用采样换准确率（对应第六章/第十二章思想）

**当前问题**
每个分支单样本输出，受单次采样随机性影响大；硬题靠"一次蒙"风险高。

**改进方案**
- 对 `systematic` 分支做 **k 次自洽采样 + 多数投票**（Self-Consistency，Wang et al. 2022，hello-agents 第六章框架思想同源）；
- 投票结果与 code_first 的确定性计算结果交叉印证，由 Arbiter 择优；
- 成本可控：仅对高价值分支采样，且 `k` 可配置。

**落地位置**：`solver.py` 增加 `sample_k` 参数，`config.py` 加 `SELF_CONSISTENCY_K`。
**优先级**：🟠 中（与 §6 置信度改造可合并做）。

---

## 8. 评估体系升级（对应第十二章 智能体性能评估）

**当前问题**
评测只有 Accuracy + CE 两个数，n=20 小样本下置信区间宽（±21.8%），无法定位弱项；答案提取曾出现"集合漏元素被判错"的严格匹配误伤。

**理论依据**
> 第十二章：
> - 评估面临"输出不确定性、标准多样性、成本高"三大挑战（12.1.1）；
> - 渐进式评估：小样本→大样本（12.2.6）；
> - GAIA 用**准精确匹配（Quasi Exact Match）**：先归一化（去单位、小写、排序）再比，缓解格式误判（12.3）；
> - 多维度 LLM Judge：正确性/清晰度/难度匹配/完整性（12.4）。

**改进方案**
- **准精确匹配**：judge 前对答案做归一化（集合排序、去空格/单位、大小写），减少格式误伤；
- **分层指标**：按题型、域、难度分别报准确率 + 置信区间；
- **校准可视化**：画 reliability diagram（置信度分箱 vs 实际正确率），把 CE=51 变成可读图；
- **Win Rate 对比**：RAG vs 无 RAG 逐题配对比，给出胜/负/平（已部分实现，固化为报告）。

**落地位置**：`run_judge_results.py` 增强。
**优先级**：🟢 中（已有基础，做归一化+分层即可）。

---

## 9. 自进化闭环：让框架越跑越聪明（对应 Extra10 Agent Self-Evolution）

**当前问题**
框架是"一次性推理"，跑完即弃，错误经验、可复用解法全部丢失。

**理论依据**
> Extra10 把自进化分为四类闭环、四阶段演进：
> ```
> 上下文/记忆自进化 → 技能资产自进化 → 群体经验自进化 → 参数/代码/工作流自修改
> ```
> 关键工程原则："评估器比生成器更重要""可回滚是自进化的基础设施""最稳妥路线是先做可解释、可评估、可回滚的记忆与技能闭环"（Extra10 总结）。

**改进方案（对齐四阶段，先低风险的）**
- **阶段一（记忆闭环）**：用 §4 的 `memory_store` 沉淀题型解法与失败经验；
- **阶段二（技能资产化）**：当某类题（如 overlap-add）反复出现，从轨迹中抽取成 `SKILL.md` 式可复用技能，带评测与回滚；
- **阶段三/四（暂不急）**：群体/参数级自修改风险高，留待评估与沙箱成熟后再做。

**落地位置**：`memory.py` + `skills/` 目录。
**优先级**：🟢 低（长期方向，先搭骨架）。

---

## 10. 工程健壮性：把"跑挂"变成"跑完"（来自实测观察）

**当前问题（实测暴露）**
- 早期 `Q1` 因 API 超时直接变成 `pipeline error`，答案全空；
- 无 RAG 版最后 3 题卡在 API 重试很久；
- 缺统一重试/退避、缺成本与延迟统计。

**理论依据**
> 第十二章鲁棒性指标：Error Rate、Failure Recovery（12.x 常用指标）；第一章强调 Agent Loop 需"自主性+容错"。

**改进方案**
- **编排层重试**：`pipeline.process_one` 对整题加指数退避重试（区别于分支内重试），失败才记 `pipeline error`；
- **熔断/降级**：RAG 检索失败自动降级 TF-IDF（代码已留 `SimpleRAGRetriever` 兜底）；
- **可观测性**：记录每题 token 数、耗时、成本，评测报告附效率指标（第十二章 Response Time / Token Usage）；
- **断点续跑**：按 `id` 记录已完成题，重跑只补缺失，避免重复烧钱（当前 `--offset` 是手动的）。

**落地位置**：`pipeline.py`（重试+续跑）、`api_client.py`（退避）、`run.py`（统计）。
**优先级**：🔴 高（保障 20/20 稳定产出，已部分具备）。

---

## 11. 演进路线图（参考 Extra10 四阶段 + 实测 ROI）

| 阶段 | 改进项 | 预期收益 | 工作量 |
|------|--------|----------|--------|
| **P0（立即）** | §2 Reflection 反思闭环 + §6 自洽置信度 | 校准 CE↓、硬题准确率↑ | 中 |
| **P0（立即）** | §10 编排层重试/续跑 | 稳定 20/20 不空答 | 小 |
| **P1（近期）** | §1 Planner 规划 + §3 ReAct 工具循环 | 多步/计算题更稳 | 中 |
| **P1（近期）** | §8 评估归一化+分层+校准图 | 评测可信、可定位弱项 | 小 |
| **P2（中期）** | §4 跨题记忆 + §5 RAG 重排/MQE | RAG 边际再提升 | 中 |
| **P3（长期）** | §7 投票 + §9 自进化技能 | 框架自迭代 | 大 |

> 原则（Extra10）：**先做好"可解释、可评估、可回滚"的记忆与技能闭环，再谈参数/工作流自修改**。每一次改动都必须能跑出新的 Accuracy/CE 对比，用 §8 的评估体系证明"没变坏"。

---

## 12. 一句话总结

当前框架是"**多分支 Workflow**"， strengths 在并发与 RAG；最大短板是**没有反思(Reflection)、没有规划(Plan)、没有记忆(Memory)、校准失控(CE≈51)**。按 hello-agents 的方法论，下一步不是堆模型，而是把单轮"生成即结束"升级为 **Plan → Act(ReAct) → Reflect → Memory** 的闭环 Agent，并用分层评估把每次改动钉在可信指标上。
