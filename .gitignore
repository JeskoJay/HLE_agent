# 密钥与本地配置（绝不入库）
.env
.env.*
!.env.example

# 工作区内部状态
.workbuddy/
.kh_proxy/

# Python
__pycache__/
*.pyc
.venv/
venv/

# 运行中间产物
.workbuddy/agent_tmp/
*.bak

# 运行结果（由流水线生成，不入库；如需共享请取消对应注释）
*.bak
HLE_text_only_20questions_responses.jsonl
HLE_solver_report.md

# 监控脚本运行日志
monitor.log
EVAL_SUMMARY.md

# 流式推理日志（实时 token 输出，运行时生成，不入库）
reasoning_stream.log

# 运行输出文件夹（每题一个子文件夹 + 汇总，不入库）
outputs/

# 本地回收站备份（删除操作的临时安全网，不入库）
.trash_*/

# 评测结果
EVAL_SUMMARY.md
